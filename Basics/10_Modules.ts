// Once this module is defined, you can use it from another module
export function add(x: number, y: number): number {
  return x + y;
}

// TypeScript allows you to name imports.
// import { add as notMyAdd } from "./math";
